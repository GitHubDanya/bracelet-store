version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    container_name: app_postgres
    env_file:
      - ./src/BraceletStore.Api/.env
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql